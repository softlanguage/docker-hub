# /etc/pgbouncer $ cat pgbouncer.ini
################## Auto generated, *=all-databases ##################
[databases]
* = host=pg14-citus port=5432 user=dev
# *=all databases

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = md5
pool_mode = session
ignore_startup_parameters = extra_float_digits
# Log settings
admin_users = postgres,dev
# Connection sanity checks, timeouts
# TLS settings
# Dangerous timeouts
################## end file ##################
